<%= simple_form_for [ @product, @review ], method: :post, remote: true do |f| %>
  <%= f.input :rating %>
  <%= f.input :content %>
  <%= f.submit label: "Add Review", class: "review-button" %>
<% end %>
